#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

# Frontend build
cd "$ROOT_DIR/frontend" && bun run build

# Backend tests
cd "$ROOT_DIR/backend" && deno task test

# Stdb tests
cd "$ROOT_DIR/stdb" && cargo test
